# Действие 1 - настроить интерфейсы на компах

Все дефолтно, настойки из прошлой лабы

# Действие 2 - настройка GRE на Cisco

R1
```text
interface Tunnel1
description "GRE-Tunnel1"
interface Tunnel1
ip address 172.10.100.1 255.255.255.252
ip mtu 1400
ip tcp adjust-mss 1360
tunnel source e0/1
tunnel destination 10.10.30.4
do wr
```

R4
```text
interface Tunnel1
ip address 172.10.100.2 255.255.255.252
ip mtu 1400
ip tcp adjust-mss 1360
tunnel source e0/0
tunnel destination 172.10.100.1
do wr
```

Еще надо добавить accept-правила на обоих роутерах, потому что действует политика default deny. Команда:
```bash
sudo iptables -I INPUT 1 -s 192.168.1.0/24 -j ACCEPT
```
Можно и для всех сетей конечно добавить, но лучше только для сетей, которые должны общаться непосредственно через GRE

```text
router ospf 1
network 172.10.100.0 0.0.0.3 area 0
do wr
```
# Действие 3 - настройка GRE на Mikrotik

```text
routing ospf instance edit numbers=0 value-name=redistribute-connected -> ставим yes
```

Создаем виртуальный интерфейс
```text
interface gre add name=tunnel1 local-address=172.10.101.7 remote-address=172.10.101.8
ip address add address=172.10.101.7/30 interface=tunnel1 network=172.10.101.6
ip route add distance=1 dst-address=10.10.50.0/24 gateway=172.10.101.8
```

### Действие 4 - настроить GRE между линуксами в сетям 172.16.2.0 и 172.16.4.0

```bash
modprobe ip_gre
lsmod | grep gre
```

должно получиться такое:
```bash
ip_gre [цифры] 0
gre [цифры] 1 ip_gre
```

На дистрибутиве должны быть установлены iptables и iproute2

Далее настраиваем туннель:
```bash
nano /etc/sysctl.conf
# раскомментировать либо прописать net.ipv4.ip_forward=1
# ctrl-o + ctrl-x
sysctl -p
# вывод - net.ipv4_ip_forward=1

sudo ip tunnel add gre1 mode gre ttl 255 local 172.10.102.2 remote 172.10.102.4
```